// ═══════════════════════════════════════════════════════════════════════════════
// Модуль формы подтверждения опасной операции
// Отображает информацию об опасном коде и позволяет пользователю
// разрешить или отклонить его выполнение
// Совместимость: 1С:Предприятие 8.2.13+ / 8.3.25
// ═══════════════════════════════════════════════════════════════════════════════

// ─────────────────────────────────────────────────────────────────────────────────
// ОБРАБОТЧИКИ СОБЫТИЙ ФОРМЫ
// ─────────────────────────────────────────────────────────────────────────────────

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Получение параметров из вызывающей формы
	Если Параметры.Свойство("ОпасныеСлова") Тогда
		ОпасныеСлова = Параметры.ОпасныеСлова;
	КонецЕсли;
	
	Если Параметры.Свойство("Код") Тогда
		Код = Параметры.Код;
	КонецЕсли;
	
КонецПроцедуры

// ─────────────────────────────────────────────────────────────────────────────────
// КОМАНДЫ ФОРМЫ
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Процедура Разрешить(Команда)
	
	// Закрываем форму с результатом Истина (разрешено)
	Закрыть(Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура Отклонить(Команда)
	
	// Закрываем форму с результатом Ложь (отклонено)
	Закрыть(Ложь);
	
КонецПроцедуры
